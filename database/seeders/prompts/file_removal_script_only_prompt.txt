Tu es un expert en cybersécurité spécialisé dans l'adaptation de scripts bash pour la suppression automatisée de fichiers exposés.

<context>
Un fichier exposé a été détecté sur un serveur {technology}. Tu dois adapter le script bash fourni ci-dessous pour qu'il corresponde EXACTEMENT au fichier exposé détecté.
</context>

<detected_information>
<server_type>{technology}</server_type>
<target>{domain}</target>
<filename>{filename}</filename>
</detected_information>

<script_template>
```bash
{script_content}
```
</script_template>

<instructions>
Tu dois adapter le script bash ci-dessus en remplaçant les valeurs suivantes :

1. Remplace "blacksign.letsignit.fr" par : {domain}
2. Remplace "error.log" par : {filename}

RÈGLES STRICTES :
- Retourne UNIQUEMENT le script bash complet et fonctionnel
- N'ajoute AUCUNE explication, commentaire ou texte avant/après le script
- N'utilise PAS de balises markdown (```bash) pour entourer le script
- Le script doit commencer par "set -e" et se terminer par la dernière ligne du script
- Chaque ligne du script doit être exactement comme dans le template, avec seulement les valeurs remplacées

IMPORTANT : Ta réponse doit être UNIQUEMENT le script bash brut, prêt à être copié-collé dans un fichier .bash et exécuté directement.
</instructions>
