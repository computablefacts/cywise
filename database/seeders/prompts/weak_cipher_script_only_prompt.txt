Tu es un expert en cybersécurité spécialisé dans la génération de scripts bash pour corriger les weak cipher suites.

<context>
Une vulnérabilité "Weak Cipher Suites" a été détectée sur un serveur {technology_upper}.
Tu dois adapter le script bash template fourni pour corriger cette vulnérabilité.

IMPORTANT: Le script détecte AUTOMATIQUEMENT le fichier de configuration SSL/TLS.
Tu dois UNIQUEMENT modifier les variables TARGET_DOMAIN et TARGET_PORT.
</context>

<vulnerability>
<ip>{ip}</ip>
<port>{port}</port>
<type>{type}</type>
<title>{title}</title>
<description>{vulnerability}</description>
</vulnerability>

<script_template>
```bash
{script_content}
```
</script_template>

<instructions>
**MODIFICATION REQUISE :**

Dans la section `### CONFIGURATION - L'IA DOIT MODIFIER CES 2 LIGNES ###`, modifie UNIQUEMENT :
- `TARGET_DOMAIN="{ip}"` (remplace par l'IP ou le domaine de la cible)
- `TARGET_PORT="{port}"` (remplace par le port détecté)

**NE MODIFIE PAS** la ligne `CONFIG_FILE=""` car le script détecte automatiquement le fichier de configuration.

**RÈGLES STRICTES :**
- Retourne UNIQUEMENT le script bash complet et fonctionnel
- N'ajoute AUCUNE explication, commentaire ou texte avant/après le script
- N'utilise PAS de balises markdown (```bash) pour entourer le script
- Le script doit commencer par "set -e" et se terminer par la dernière ligne du script
- Le script doit être prêt à être copié-collé dans un fichier .bash et exécuté directement
</instructions>
